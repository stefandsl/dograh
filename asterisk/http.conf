; HTTP server — required by ARI.
; Bind to all interfaces so the api container can reach ARI on the
; docker network. The ARI port is NOT exposed to the host in
; docker-compose.override.yaml — only the SIP/RTP ports are.
[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
prefix =
