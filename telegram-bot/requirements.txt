# Telegram framework (ADR-100)
aiogram>=3.28,<4

# HTTP to Dograh API (ADR-102, X-API-Key)
httpx[http2]>=0.27,<1

# Postgres for sessions + memory + scheduler (ADR-103)
asyncpg>=0.29,<1
sqlalchemy[asyncio]>=2,<3

# Multi-bot hot-reload signalling (Phase 4)
redis>=5,<6

# Scheduler (Phase 2 may swap CliClaw's custom cron for this)
apscheduler>=3.10,<4

# Voice-note STT (Phase 3 path 1)
groq>=0.11,<1

# Fernet-signed WebApp links (Phase 3 path 2)
cryptography>=43,<44

# Healthcheck server
aiohttp>=3.9,<4

# Env loading
python-dotenv>=1,<2

# Structured logging
loguru>=0.7,<1
